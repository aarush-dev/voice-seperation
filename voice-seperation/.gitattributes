# Normalize to LF in the repo; check out native on Windows.
* text=auto

*.sh   text eol=lf
*.py   text
*.md   text
*.html text
*.js   text

# Binary caches — never touch these.
*.npz  binary
*.wav  binary
*.flac binary
